material grid
{
    technique
    {
        pass 0
        {
            // shaders
            vertexShader = res/line.vsh
            fragmentShader = res/line.fsh

             // uniforms
            u_worldViewProjectionMatrix = WORLD_VIEW_PROJECTION_MATRIX
            
            // render state
            renderState
            {
                cullFace = true
                depthTest = true
            }
        }
    }
}